---
layout: base
---

<div class="intro">
  <h1 class="site-heading">Untitled <span class="tag katharine">Katharine</span> and <span class="tag salomone">Salomone</span> Music Listening Project</h1>
  <p>A definitely successful attempt to make our way through the <a href="https://www.rollingstone.com/music/music-lists/best-albums-of-all-time-1062063/" target="_blank">2020 Rolling Stone "500 Greatest Albums"</a> list. A new random album each week.</p>
</div>

<select-filter></select-filter>
<ul class="album__list" data-filter="all">
  {%- for row in spreadsheet %}
    {% import "partials/row.njk" as rowPartial %}
    {{ rowPartial.album(row) }}
  {% endfor %}
</ul>